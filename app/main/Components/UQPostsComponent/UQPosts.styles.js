import {StyleSheet} from 'react-native';

const styleObject = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    height: '100%',
    width: '100%',
  }
});

export default styles = StyleSheet.create(styleObject);
